/** Shared styling for plain `<input>` / `<textarea>` form fields — one string
 *  so a focus-ring tweak can never drift between the windows that use it. */
export const FIELD =
  "w-full rounded-md border bg-background px-3 py-1.5 text-sm outline-none focus-visible:border-ring focus-visible:ring-[3px] focus-visible:ring-ring/50";
